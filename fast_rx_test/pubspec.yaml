name: fast_rx_test
description: Testing utilities for fast_rx. Check for valid RxObject registration.
version: 0.1.0
homepage: https://github.com/Rexios80/fast_ui/tree/master/fast_rx_test

environment:
  sdk: ">=2.16.1 <3.0.0"
  flutter: ">=1.17.0"

dependencies:
  flutter:
    sdk: flutter

  fast_rx: ^0.2.0
  mockito: ^5.0.17

dev_dependencies:
  rexios_lints: ^1.0.0
  build_runner: ^2.1.7
