name: fast_rx_test
description: Testing utilities for fast_rx. Check for valid rx registration and notifications.
version: 0.4.0
homepage: https://github.com/Rexios80/fast_ui/tree/master/fast_rx_test

environment:
  sdk: ">=2.17.0 <4.0.0"

dependencies:
  fast_rx:
    path: ../fast_rx
  test: ^1.19.0

dev_dependencies:
  rexios_lints: ^5.0.0
