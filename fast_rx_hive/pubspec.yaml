name: fast_rx_hive
description: A Hive implementation of fast_rx_persistence storage for Flutter
version: 0.1.0
homepage: https://github.com/Rexios80/fast_ui/tree/master/fast_rx_hive

environment:
  sdk: ">=2.17.0 <3.0.0"
  flutter: ">=1.17.0"

dependencies:
  flutter:
    sdk: flutter

  fast_rx_persistence: ^0.1.0
  hive_flutter: ^1.1.0

dev_dependencies:
  test: ^1.21.1
  rexios_lints: ^3.0.0
  fast_rx: ^0.2.7
