name: fast_rx
description: An easy to understand reactive state management solution for Flutter
version: 0.3.0
homepage: https://github.com/Rexios80/fast_ui/tree/master/fast_rx

environment:
  sdk: ">=2.17.0 <3.0.0"
  flutter: ">=1.17.0"

dependencies:
  flutter:
    sdk: flutter

dev_dependencies:
  flutter_test:
    sdk: flutter

  rexios_lints: ^3.0.0
  mockito: ^5.3.2
  fast_rx_test: ^0.3.0

dependency_overrides:
  fast_rx_test:
    git:
      url: https://github.com/Rexios80/fast_ui
      path: fast_rx_test
      ref: 1f439f50db48f0b3a68a1a8cb4f64d550b2f6c3a
