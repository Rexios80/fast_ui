name: fast_rx_flutter
description: Reactive Flutter widgets and convenience components for fast_rx
version: 0.1.1
homepage: https://github.com/Rexios80/fast_ui/tree/master/fast_rx_flutter

environment:
  sdk: ">=2.18.0 <3.0.0"
  flutter: ">=1.17.0"

dependencies:
  flutter:
    sdk: flutter

  fast_rx: ^0.4.1

dev_dependencies:
  flutter_test:
    sdk: flutter

  rexios_lints: ^4.0.0

dependency_overrides:
  fast_rx:
    path: ../fast_rx