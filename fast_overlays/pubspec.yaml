name: fast_overlays
description: Show overlays, dialogs, snackbars, and more without a context
version: 0.1.5
homepage: https://github.com/Rexios80/fast_ui/tree/master/fast_overlays

environment:
  sdk: ^3.0.0
  flutter: ">=1.17.0"

dependencies:
  flutter:
    sdk: flutter

dev_dependencies:
  flutter_test:
    sdk: flutter

  rexios_lints: ^9.1.0
