name: fast_state_example
description: A new Flutter project.
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: ">=2.15.1 <3.0.0"

dependencies:
  flutter:
    sdk: flutter
  
  fast_state:
    path: ../

dev_dependencies:
  flutter_test:
    sdk: flutter

  rexios_lints: ^1.0.0